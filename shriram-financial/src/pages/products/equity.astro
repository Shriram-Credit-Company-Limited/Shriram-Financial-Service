---
import BaseLayout from '../../layouts/BaseLayout.astro';

const equityTypes = [
  {
    label: 'Common Equity Shares',
    desc: 'The most widely traded shares. Investors receive voting rights and participate in the company\'s profits through dividends and price appreciation.',
  },
  {
    label: 'Preferred Equity Shares',
    desc: 'Preferred shareholders receive preference in dividend payments over common shareholders. These shares generally carry limited or no voting rights.',
  },
];

const eligibility = [
  'Indian resident (NRIs can invest through specific routes)',
  'Valid PAN card mandatory for all account holders',
  'Active bank account in your name',
  'Completed KYC verification as per SEBI regulations',
  '18+ years of age (minors can invest through guardians)',
];

const documents = [
  'PAN card (mandatory)',
  'Aadhaar card for identity verification',
  'Address proof — Aadhaar, Passport, or Utility Bill',
  'Cancelled cheque or recent bank statement',
  'Passport-size photograph (JPEG format)',
  'Valid mobile number and email ID',
];

const steps = [
  { n: '1', title: 'Open Demat & Trading Account', desc: 'Open a Demat and trading account online with Shriram in minutes.' },
  { n: '2', title: 'Complete PAN / KYC / Bank Linking', desc: 'Submit your PAN, complete KYC with Aadhaar and link your bank account.' },
  { n: '3', title: 'Search and Analyse Stocks', desc: 'Use research tools, screeners and expert analysis to identify quality stocks.' },
  { n: '4', title: 'Buy, Hold, Sell & Track', desc: 'Execute trades, build your portfolio and track holdings to grow your wealth.' },
];

const brokerageRows = [
  { label: 'Brokerage',        delivery: '₹0 (Zero)',                         intraday: '₹20 per executed order',         fo: '₹20 per executed order' },
  { label: 'STT',              delivery: '0.1% on sell side',                  intraday: '0.025% on buy & sell side',      fo: '0.02% on sell side' },
  { label: 'GST',              delivery: '18% on brokerage + exchange charges', intraday: '18% on brokerage + exchange charges', fo: '18% on brokerage + exchange charges' },
  { label: 'DP Charges',       delivery: '₹15.93 per scrip on sell',           intraday: 'N/A',                            fo: 'N/A' },
  { label: 'Exchange Charges', delivery: '0.00345% (NSE) / 0.00375% (BSE)',    intraday: '0.00345% (NSE) / 0.00375% (BSE)', fo: '0.002%' },
];

const taxRows = [
  { feature: 'Holding Period',       stcg: 'Up to 12 months',          ltcg: 'More than 12 months' },
  { feature: 'Tax Rate',             stcg: '20% Flat',                  ltcg: '12.5%' },
  { feature: 'Tax-Free Limit',       stcg: 'Nil (taxed from ₹1)',       ltcg: 'Up to ₹1.25 Lakh per year' },
  { feature: 'Indexation Benefit',   stcg: 'Not Applicable',            ltcg: 'Not Applicable' },
  { feature: 'Applicable Section',   stcg: 'Section 111A',              ltcg: 'Section 112A' },
];

const categories = [
  { label: 'Large Cap',       desc: 'Top 100 companies by market capitalisation — stable and lower risk.' },
  { label: 'Mid Cap',         desc: 'Companies ranked 101–250 — a balance of growth potential and stability.' },
  { label: 'Small Cap',       desc: 'Companies ranked beyond 250 — higher growth potential with higher risk.' },
  { label: 'Growth Stocks',   desc: 'Companies expected to grow earnings faster than the broader market.' },
  { label: 'Dividend Stocks', desc: 'Companies that share regular profits with shareholders as dividends.' },
  { label: 'Value Stocks',    desc: 'Stocks trading below their intrinsic worth — potentially undervalued.' },
  { label: 'Blue Chip',       desc: 'Well-established, financially sound market leaders with a strong record.' },
  { label: 'Cyclical Stocks', desc: 'Stocks whose performance moves in line with broader economic cycles.' },
];

const research = [
  { title: 'Fundamental Research', idx: 'Approach 01', desc: 'In-depth company analysis, financial modelling and valuation reports to assess a stock\'s true intrinsic worth.', href: '/research/' },
  { title: 'Technical Research',   idx: 'Approach 02', desc: 'Chart patterns, price action and technical indicators to time entries and exits with greater precision.',         href: '/research/' },
];

const strategyRows = [
  { label: 'Holding Period',   intraday: 'Same day (square-off before close)', swing: '2 days to a few weeks',       lt: '1+ years (often 5–10+ years)' },
  { label: 'Time Commitment',  intraday: 'High — active monitoring',           swing: 'Moderate — daily/weekly check', lt: 'Low — periodic review' },
  { label: 'Capital Required', intraday: 'Lower entry, margin common',         swing: 'Moderate',                     lt: 'Can start small via SIP (₹500+)' },
  { label: 'Risk Level',       intraday: 'Very High',                          swing: 'High',                         lt: 'Moderate to Low' },
];

const vsRows = [
  { feature: 'Control',              equity: 'Direct ownership and control',          mf: 'Fund manager decides' },
  { feature: 'Risk',                 equity: 'Company-specific risk',                 mf: 'Diversified risk' },
  { feature: 'Return Potential',     equity: 'Potentially higher (unlimited upside)', mf: 'Market-linked returns' },
  { feature: 'Research Involvement', equity: 'Active research needed',                mf: 'Professional management' },
  { feature: 'Suitable For',         equity: 'Knowledgeable investors',               mf: 'All investor types' },
  { feature: 'Cost',                 equity: 'Brokerage per trade',                   mf: 'Expense ratio annually' },
  { feature: 'Liquidity',            equity: 'Real-time during market hours',         mf: 'End-of-day NAV' },
  { feature: 'Management',           equity: 'Self-managed portfolio',                mf: 'Professionally managed' },
];

const risks = [
  { label: 'Market Risk',         desc: 'Broad market movements can affect all stocks due to economic events, policy changes or global sentiment.' },
  { label: 'Volatility',          desc: 'Prices can fluctuate significantly within short periods, testing investor discipline and patience.' },
  { label: 'Stock-Specific Risk', desc: 'Company-specific factors such as poor earnings or governance issues can impact a single stock sharply.' },
  { label: 'Behavioural Risk',    desc: 'Emotional decisions driven by fear or greed can lead to poor timing and avoidable losses.' },
];

const faqs = [
  { q: 'What is the difference between large-cap, mid-cap and small-cap stocks?', a: 'Large-cap stocks are the top 100 companies by market capitalisation and tend to be stable. Mid-caps (ranked 101–250) balance growth and stability, while small-caps (beyond 250) offer higher growth potential at higher risk.' },
  { q: 'How is stock market profit taxed in India in 2026?', a: 'Short-term capital gains (held up to 12 months) are taxed at 20% flat, while long-term gains (held over 12 months) are taxed at 12.5% beyond the ₹1.25 lakh annual exemption.' },
  { q: 'What is a P/E ratio and why does it matter for investors?', a: 'The price-to-earnings (P/E) ratio compares a stock\'s price to its earnings per share. It helps investors gauge whether a stock is over- or under-valued relative to its peers and historical levels.' },
  { q: 'Should I invest a lump sum or use SIP for equity investments?', a: 'A SIP spreads investments over time and averages out market volatility, making it ideal for most investors. Lump-sum investing can work when markets are favourable and you have a longer horizon and higher risk appetite.' },
  { q: 'How do I evaluate if a stock is fundamentally strong?', a: 'Assess consistent revenue and profit growth, low debt, healthy return ratios (ROE / ROCE), strong cash flows and competent management. Combine these fundamentals with reasonable valuations before investing.' },
  { q: 'What is the minimum amount needed to start equity investing?', a: 'No regulatory minimum exists. You can start with the price of one share — many blue-chips trade under ₹500. Zero delivery brokerage at Shriram means no extra cost for buy-and-hold investors.' },
];
---

<BaseLayout
  title="Buy Equity Stocks India — Zero Brokerage NSE BSE Trading | Shriram"
  description="Buy equity stocks India on NSE & BSE with zero delivery brokerage, SEBI-registered research, and live market data. Start equity investing in India today with Shriram Financial Services."
>

  <!-- ══ 1. HERO ══════════════════════════════════════════════════════ -->
  <section class="hero">
    <div class="hero-inner">
      <h1>Build Long-Term Wealth Through <span class="g">Equity Investing</span></h1>
      <p class="lead">Buy and invest in equity stocks in India with live market data, deep research insights, stock analysis tools, and expert-backed guidance from Shriram Financial Services.</p>
      <div class="cta-row">
        <a class="btn-gold" href="/about/open-demat-account/">Open Demat Account</a>
        <a class="btn-ghost" href="/about/support/">Talk to RM</a>
      </div>
      <div class="hero-feats">
        <div class="feats">
          <div class="feat"><span class="tk">✓</span><span class="lb">&nbsp;SEBI-registered</span></div>
          <div class="feat"><span class="tk">✓</span><span class="lb">&nbsp;Research-led</span></div>
          <div class="feat"><span class="tk">✓</span><span class="lb">&nbsp;Full-service</span></div>
          <div class="feat"><span class="tk">✓</span><span class="lb">&nbsp;Transparent</span></div>
        </div>
      </div>
    </div>
  </section>

  <!-- ══ 2. WHAT ARE EQUITY STOCKS ════════════════════════════════════ -->
  <section class="section stack" id="sec-about" style="background:var(--sec-light);gap:56px">
    <div class="stack" style="gap:16px">
      <p class="sec-title">What Are Equity Stocks?</p>
      <p class="sec-lead">Equity stocks, also known as shares, represent ownership in a company. When you purchase equity shares, you become a partial owner of that company and gain rights to a portion of its assets and earnings. In India, equity stocks trade on two major exchanges, with all activity regulated by the Securities and Exchange Board of India (SEBI) to protect investor interests.</p>
    </div>
    <div class="stack" style="gap:32px">
      <div class="stack" style="gap:16px">
        <p class="sub-title">Where Equity Stocks Trade</p>
        <p class="sub-lead">The market functions through two primary exchanges that enable transparent, electronic trading of shares.</p>
      </div>
      <div class="row-2">
        <div class="card" style="border-color:var(--card-border-soft)">
          <p class="card-h">National Stock Exchange</p>
          <p class="card-p">India's largest exchange by trading volume, offering electronic trading across equities, derivatives and debt instruments with high liquidity and transparent price discovery.</p>
        </div>
        <div class="card" style="border-color:var(--card-border-soft)">
          <p class="card-h">Bombay Stock Exchange</p>
          <p class="card-p">Asia's oldest stock exchange, home to thousands of listed companies and the benchmark Sensex index, providing a deep and well-regulated marketplace for investors.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- ══ 3. TYPES OF EQUITY STOCKS ════════════════════════════════════ -->
  <section class="section stack" id="sec-types" style="background:var(--sec-tint);gap:56px;justify-content:center">
    <div class="stack" style="gap:16px">
      <p class="sec-title">Types of Equity Stocks</p>
      <p class="sec-lead">Stocks are grouped two ways — by the rights they carry, and by the size and style of the company behind them.</p>
    </div>

    <div class="stack" style="gap:32px">
      <div class="stack" style="gap:16px">
        <p class="sub-title">By Shareholder Rights</p>
        <p class="sub-lead">Companies issue different classes of shares, each carrying its own rights to voting, dividends and profits.</p>
      </div>
      <div class="row-2">
        {equityTypes.map(t => (
          <div class="card">
            <p class="card-h">{t.label}</p>
            <p class="card-p">{t.desc}</p>
          </div>
        ))}
      </div>
    </div>

    <div class="stack" style="gap:32px">
      <div class="stack" style="gap:16px">
        <p class="sub-title">By Size and Style</p>
        <p class="sub-lead">The main categories investors use to classify and compare stocks.</p>
      </div>
      <div class="cat-grid">
        {categories.map(c => (
          <div class="cat"><h4>{c.label}</h4><p>{c.desc}</p></div>
        ))}
      </div>
    </div>

    <a class="btn-gold" href="/about/open-demat-account/" style="align-self:center">Open Demat Account</a>
  </section>

  <!-- ══ 4. HOW TO START INVESTING ════════════════════════════════════ -->
  <section class="section stack" id="sec-getting-started" style="background:var(--sec-light);gap:56px;align-items:center;justify-content:center">
    <div class="stack" style="gap:16px;width:100%">
      <p class="sec-title">How to Start Investing</p>
      <p class="sec-lead">Four simple steps from sign-up to your first trade — plus exactly what you'll need to qualify.</p>
    </div>

    <div class="steps">
      {steps.map((s, i) => (
        <>
          {i > 0 && <div class="step-div"></div>}
          <div class="step">
            <div class="num">{s.n}</div>
            <div class="st-body">
              <h4>{s.title}</h4>
              <p>{s.desc}</p>
            </div>
          </div>
        </>
      ))}
    </div>

    <div class="stack" style="gap:32px;width:100%">
      <div class="stack" style="gap:16px">
        <p class="sub-title">Eligibility &amp; Documents</p>
        <p class="sub-lead">Everything you need to qualify and complete your account opening.</p>
      </div>
      <div class="row-2">
        <div class="card check-card">
          <p class="ch-title">Eligibility Criteria</p>
          <div class="check-list">
            {eligibility.map((item, i) => (
              <>
                {i > 0 && <div class="check-sep"></div>}
                <div class="check-row"><span class="ck">✓</span><p class="tx">{item}</p></div>
              </>
            ))}
          </div>
        </div>
        <div class="card check-card">
          <p class="ch-title">Documents Required</p>
          <div class="check-list">
            {documents.map((item, i) => (
              <>
                {i > 0 && <div class="check-sep"></div>}
                <div class="check-row"><span class="ck">✓</span><p class="tx">{item}</p></div>
              </>
            ))}
          </div>
        </div>
      </div>
    </div>

    <a class="btn-gold" href="/about/open-demat-account/" style="align-self:center">Open Demat Account</a>
  </section>

  <!-- ══ 5. COSTS & TAXES ══════════════════════════════════════════════ -->
  <section class="section stack" id="sec-costs" style="background:var(--sec-tint);gap:56px;justify-content:center">
    <div class="stack" style="gap:16px">
      <p class="sec-title">Costs &amp; Taxes</p>
      <p class="sec-lead" style="color:var(--ink-2)">What you pay to trade, and how your gains are taxed — with no hidden costs.</p>
    </div>

    <div class="stack" style="gap:32px">
      <div class="stack" style="gap:16px">
        <p class="sub-title">Brokerage &amp; Charges</p>
        <p class="sub-lead" style="color:var(--ink-2)">A clear breakdown of the charges that apply across delivery, intraday and F&amp;O segments.</p>
      </div>
      <div class="t-scroll">
        <div class="table t4">
          <div class="cell th"><p>Charge Type</p></div>
          <div class="cell th"><p>Delivery</p></div>
          <div class="cell th"><p>Intraday</p></div>
          <div class="cell th"><p>F&amp;O</p></div>
          {brokerageRows.map(r => (
            <>
              <div class="cell tb"><p class="k">{r.label}</p></div>
              <div class="cell tb"><p class="v">{r.delivery}</p></div>
              <div class="cell tb"><p class="v">{r.intraday}</p></div>
              <div class="cell tb"><p class="v">{r.fo}</p></div>
            </>
          ))}
        </div>
      </div>
    </div>

    <div class="stack" style="gap:32px">
      <div class="stack" style="gap:16px">
        <p class="sub-title">Tax on Equity Stocks — LTCG &amp; STCG</p>
        <p class="sub-lead" style="color:var(--ink-2)">How short-term and long-term capital gains are treated for equity investors.</p>
      </div>
      <div class="t-scroll">
        <div class="table t3">
          <div class="cell th"><p>Feature</p></div>
          <div class="cell th"><p>Short-Term (STCG)</p></div>
          <div class="cell th"><p>Long-Term (LTCG)</p></div>
          {taxRows.map(r => (
            <>
              <div class="cell tb"><p class="k">{r.feature}</p></div>
              <div class="cell tb"><p class="v">{r.stcg}</p></div>
              <div class="cell tb"><p class="v">{r.ltcg}</p></div>
            </>
          ))}
        </div>
      </div>
    </div>

    <a class="btn-gold" href="/about/open-demat-account/" style="align-self:center">Open Demat Account</a>
  </section>

  <!-- ══ 6. ANALYSE & DECIDE ═══════════════════════════════════════════ -->
  <section class="section stack" id="sec-analyse" style="background:var(--sec-light);gap:56px;justify-content:center">
    <div class="stack" style="gap:16px">
      <p class="sec-title">Analyse and Decide</p>
      <p class="sec-lead" style="color:var(--ink-2)">How to research a stock, choose a strategy, compare your options and weigh the risks.</p>
    </div>

    <div class="stack" style="gap:32px">
      <div class="stack" style="gap:16px">
        <p class="sub-title">Research Approaches</p>
        <p class="sub-lead" style="color:var(--ink-2)">Two complementary approaches, backed by data-driven insights and research-led ideas.</p>
      </div>
      <div class="approaches">
        {research.map(r => (
          <a class="appr" href={r.href}>
            <div class="appr-main">
              <p class="idx">{r.idx}</p>
              <div class="appr-tt">
                <h4>{r.title}</h4>
                <p>{r.desc}</p>
              </div>
            </div>
            <div class="explore"><span>Explore&nbsp;</span><span>→</span></div>
          </a>
        ))}
      </div>
    </div>

    <div class="stack" style="gap:32px">
      <div class="stack" style="gap:16px">
        <p class="sub-title">Top Trading Strategies</p>
        <p class="sub-lead" style="color:var(--ink-2)">Match your time, capital and risk appetite to the right approach.</p>
      </div>
      <div class="t-scroll">
        <div class="table t4 bordered">
          <div class="cell th"><p>Parameter</p></div>
          <div class="cell th"><p>Intraday Trading</p></div>
          <div class="cell th"><p>Swing Trading</p></div>
          <div class="cell th"><p>Long-Term Investing</p></div>
          {strategyRows.map(r => (
            <>
              <div class="cell tb"><p class="k">{r.label}</p></div>
              <div class="cell tb"><p class="v">{r.intraday}</p></div>
              <div class="cell tb"><p class="v">{r.swing}</p></div>
              <div class="cell tb"><p class="v">{r.lt}</p></div>
            </>
          ))}
        </div>
      </div>
    </div>

    <div class="stack" style="gap:32px">
      <div class="stack" style="gap:16px">
        <p class="sub-title">Equity Stocks vs Mutual Funds</p>
        <p class="sub-lead" style="color:var(--ink-2)">Compare direct equity investing with mutual funds across the factors that matter most.</p>
      </div>
      <div class="t-scroll">
        <div class="table t3 bordered">
          <div class="cell th"><p>Feature</p></div>
          <div class="cell th"><p>Equity Stocks</p></div>
          <div class="cell th"><p>Mutual Funds</p></div>
          {vsRows.map(r => (
            <>
              <div class="cell tb"><p class="k">{r.feature}</p></div>
              <div class="cell tb"><p class="v">{r.equity}</p></div>
              <div class="cell tb"><p class="v">{r.mf}</p></div>
            </>
          ))}
        </div>
      </div>
    </div>

    <div class="stack" style="gap:32px">
      <div class="stack" style="gap:16px">
        <p class="sub-title">Risks to Weigh</p>
        <p class="sub-lead" style="color:var(--ink-2)">Understand what can move against you before you commit capital.</p>
      </div>
      <div class="risks">
        {risks.map(r => (
          <div class="risk">
            <h4>{r.label}</h4>
            <p>{r.desc}</p>
          </div>
        ))}
      </div>
    </div>

    <a class="btn-gold" href="/about/open-demat-account/" style="align-self:center">Open Demat Account</a>
  </section>

  <!-- ══ 7. GENERAL QUESTIONS (FAQ) ═══════════════════════════════════ -->
  <section class="faq-wrap" id="sec-faq" style="background:var(--sec-tint)">
    <div class="faq-cols">
      <div class="faq-left">
        <p class="faq-title">General Questions</p>
        <div class="faq-acc">
          {faqs.map((item, i) => (
            <div class={`faq-item${i === 0 ? ' open' : ''}`}>
              <div class="q">
                <span class="qt">{item.q}</span>
                <div class="qa-wrap"><div class="qa"><p>{item.a}</p></div></div>
              </div>
              <span class="faq-icon">
                <svg viewBox="0 0 24 24" fill="none">
                  <line class="v" x1="12" y1="5" x2="12" y2="19" stroke="#404040" stroke-width="2" stroke-linecap="round"/>
                  <line x1="5" y1="12" x2="19" y2="12" stroke="#404040" stroke-width="2" stroke-linecap="round"/>
                </svg>
              </span>
            </div>
          ))}
        </div>
      </div>

      <div class="faq-side">
        <div class="usr" aria-hidden="true">
          <img src="/user-icon.png" alt="" />
        </div>
        <div class="side-in">
          <div class="side-tx">
            <h4>Need A Clearer Direction?</h4>
            <p>Let's get in touch on a 15-minute call where we would be able to answer any questions you may have. Our Advisor will additionally help map your wealth mix and demonstrate what a dedicated advisor would do differently.</p>
          </div>
          <a class="btn-gold" href="/about/support/">Book A Discovery Call</a>
        </div>
      </div>
    </div>
  </section>

  <!-- ══ 8. FINAL CTA ══════════════════════════════════════════════════ -->
  <section class="cta-sec" id="sec-final-cta" style="background:var(--sec-light)">
    <div class="cta-box">
      <div class="ct-tt">
        <h3>Open Your Demat Account Within Minutes</h3>
        <p class="ct-sub">Start investing in stocks, mutual funds and more — with zero paperwork.</p>
      </div>
      <a class="btn-gold" href="/about/open-demat-account/">Open Demat Account</a>
    </div>
  </section>

</BaseLayout>

<style>
  /* ── Design tokens ─────────────────────────────────────────────── */
  :root {
    --page: #fcf8ef;
    --sec-light: #fef8ee;
    --sec-tint: #f6efe1;
    --card: #fffdf8;
    --card-border: #e1e0db;
    --card-border-soft: rgba(28,28,22,.13);
    --row-line: rgba(28,28,22,.08);
    --olive-title: #2e3914;
    --olive-head: #2d3813;
    --olive-deep: #1b2010;
    --ink: #1c1c16;
    --ink-2: #46463c;
    --ink-3: #595959;
    --muted: #73736a;
    --muted-2: #9a9a8e;
    --gold: #e0a82e;
    --gold-2: #f2b83f;
    --gold-btn: #f3b83f;
    --gold-ink: #231a06;
    --olive-ink: #2e3914;
    --green: #009d58;
    --blue: #2f7ef4;
    --pad: clamp(20px, 8vw, 144px);
    --container-pad: 32px;
    --section-y: 72px;
    --cta-w: 220px;
  }

  /* ── Base ────────────────────────────────────────────────────────── */
  *, *::before, *::after { box-sizing: border-box; }
  a { color: inherit; text-decoration: none; }
  p { line-height: 1.5; }

  /* ── Section shell ───────────────────────────────────────────────── */
  .section {
    padding: var(--section-y) var(--pad);
    min-height: 100vh;
    min-height: 100dvh;
    display: flex;
    flex-direction: column;
    justify-content: center;
  }
  #sec-about { min-height: 0; }
  .stack { display: flex; flex-direction: column; }

  /* ── Typography ──────────────────────────────────────────────────── */
  .sec-title  { font-weight: 400; font-size: 40px; color: var(--olive-title); text-transform: capitalize; line-height: 1.5; }
  .sec-lead   { font-weight: 400; font-size: 16px; color: var(--ink-3); line-height: 1.5; }
  .sub-title  { font-weight: 500; font-size: 24px; color: var(--ink); line-height: 1.5; }
  .sub-lead   { font-weight: 400; font-size: 16px; color: var(--ink-3); line-height: 1.5; }
  .card-h     { font-weight: 500; font-size: 20px; color: var(--ink); line-height: 1.5; }
  .card-p     { font-weight: 400; font-size: 16px; color: var(--muted); line-height: 1.5; }

  /* ── Cards ───────────────────────────────────────────────────────── */
  .card {
    background: var(--card);
    border: 1px solid var(--card-border);
    border-radius: 24px;
    padding: var(--container-pad);
    display: flex;
    flex-direction: column;
    gap: 16px;
  }

  /* ── Hero ────────────────────────────────────────────────────────── */
  .hero {
    padding: 0 var(--pad);
    display: flex;
    flex-direction: column;
    justify-content: center;
    overflow: hidden;
    background: radial-gradient(125% 210% at 100% -8%, #4f583b 0%, #363a2c 50%, #1d1d1d 100%);
  }
  .hero-inner { padding: clamp(80px,12vw,140px) 0 clamp(48px,6vw,80px); display: flex; flex-direction: column; }
  .hero h1 { font-weight: 500; font-size: 40px; color: #fff; line-height: 1.5; max-width: 860px; }
  .hero h1 .g { color: var(--gold-2); }
  .hero .lead { margin-top: 32px; font-weight: 400; font-size: 18px; line-height: 1.5; color: rgba(255,255,255,.74); max-width: 600px; }
  .cta-row { margin-top: 40px; display: flex; flex-wrap: wrap; gap: 14px; align-items: center; }
  .hero-feats { margin-top: 40px; }
  .feats { display: flex; flex-wrap: wrap; align-items: center; gap: 14px 16px; max-width: 760px; padding-top: 36px; border-top: 1px solid rgba(255,255,255,.16); }
  .feat { display: flex; gap: 10px; align-items: center; }
  .feat .tk { color: var(--gold-2); font-weight: 500; font-size: 15px; width: 13px; }
  .feat .lb { color: rgba(255,255,255,.9); font-weight: 500; font-size: 16px; line-height: 1.5; white-space: nowrap; }

  /* ── Buttons ─────────────────────────────────────────────────────── */
  .btn-gold {
    background: var(--gold-btn);
    height: 40px;
    width: var(--cta-w);
    border-radius: 4px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 7px 20px;
    font-weight: 500;
    font-size: 12px;
    color: var(--olive-ink);
    white-space: nowrap;
    transition: opacity .2s;
  }
  .btn-gold:hover { opacity: .88; }
  .btn-ghost {
    height: 40px;
    border-radius: 9px;
    border: 1px solid rgba(255,255,255,.4);
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 7px 26px;
    font-weight: 600;
    font-size: 15px;
    color: #fff;
    transition: border-color .2s;
  }
  .btn-ghost:hover { border-color: #fff; }

  /* ── Two-card row ────────────────────────────────────────────────── */
  .row-2 { display: flex; gap: 32px; align-items: stretch; }
  .row-2 .card { flex: 1 0 0; min-width: 0; }

  /* ── Category grid ───────────────────────────────────────────────── */
  .cat-grid {
    border: 1px solid var(--card-border);
    border-radius: 24px;
    overflow: hidden;
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    background: var(--card);
  }
  .cat {
    background: var(--card);
    padding: var(--container-pad);
    display: flex;
    flex-direction: column;
    gap: 12px;
    border-right: 1px solid var(--card-border);
    border-bottom: 1px solid var(--card-border);
  }
  .cat:nth-child(4n)    { border-right: 0; }
  .cat:nth-child(n+5)   { border-bottom: 0; }
  .cat h4 { font-weight: 500; font-size: 20px; color: var(--ink); line-height: 1.5; }
  .cat p  { font-weight: 400; font-size: 16px; color: var(--muted); line-height: 1.5; }

  /* ── Steps ───────────────────────────────────────────────────────── */
  .steps {
    background: var(--card);
    border: 1px solid var(--card-border);
    border-radius: 24px;
    overflow: hidden;
    display: flex;
    align-items: stretch;
    width: 100%;
  }
  .step { flex: 1 0 0; min-width: 0; padding: var(--container-pad); display: flex; flex-direction: column; gap: 16px; }
  .step .num { font-weight: 400; font-size: 32px; line-height: 34px; color: var(--gold); }
  .step .st-body { display: flex; flex-direction: column; gap: 12px; }
  .step h4 { font-weight: 500; font-size: 20px; color: var(--ink); line-height: 1.5; }
  .step p  { font-weight: 400; font-size: 16px; color: var(--ink-3); line-height: 1.5; }
  .step-div { width: 1px; align-self: stretch; background: var(--card-border); flex: none; }

  /* ── Checklist cards ─────────────────────────────────────────────── */
  .check-card { flex: 1 0 0; min-width: 0; display: flex; flex-direction: column; gap: 32px; }
  .ch-title   { font-weight: 500; font-size: 20px; color: var(--ink); line-height: 1.5; }
  .check-list { display: flex; flex-direction: column; gap: 24px; }
  .check-row  { display: flex; gap: 14px; align-items: flex-start; }
  .check-row .ck { color: var(--green); font-weight: 700; font-size: 15px; line-height: 25.5px; width: 13px; flex: none; }
  .check-row .tx { flex: 1 0 0; min-width: 0; font-weight: 400; font-size: 16px; color: var(--ink-3); line-height: 1.5; }
  .check-sep  { height: 1px; background: var(--row-line); width: 100%; }

  /* ── Tables ──────────────────────────────────────────────────────── */
  .t-scroll   { width: 100%; overflow-x: auto; -webkit-overflow-scrolling: touch; }
  .table      { background: var(--card); border-radius: 24px; overflow: hidden; display: grid; }
  .table.bordered { border: 1px solid var(--card-border); }
  .t4 { grid-template-columns: repeat(4, minmax(0,1fr)); }
  .t3 { grid-template-columns: repeat(3, minmax(0,1fr)); }
  .cell { padding: 16px 24px; display: flex; align-items: center; justify-content: center; text-align: center; }
  .cell p { flex: 1 0 0; min-width: 0; font-size: 16px; line-height: 1.5; overflow-wrap: break-word; }
  .th { background: var(--olive-head); }
  .th p { font-weight: 600; color: #fff; }
  .tb { border-top: 1px solid var(--row-line); }
  .tb .k { font-weight: 600; color: var(--ink); }
  .tb .v { font-weight: 400; color: var(--ink-2); }

  /* ── Research / approach cards ───────────────────────────────────── */
  .approaches { display: flex; gap: 24px; align-items: stretch; }
  .appr {
    flex: 1 0 0; min-width: 0;
    background: var(--olive-deep);
    border: 1px solid var(--olive-deep);
    border-radius: 24px;
    padding: var(--container-pad);
    display: flex; gap: 32px; align-items: flex-start;
    transition: background .25s ease, border-color .25s ease, transform .25s ease, box-shadow .25s ease;
  }
  .appr:hover { background: #26301a; border-color: #3a471b; transform: translateY(-4px); box-shadow: 0 18px 40px rgba(27,32,16,.28); }
  .appr:hover .explore span:last-child { transform: translateX(6px); }
  .appr .explore span:last-child { transition: transform .25s ease; }
  .appr-main { flex: 1 0 0; min-width: 0; display: flex; flex-direction: column; gap: 32px; }
  .appr .idx { font-weight: 400; font-size: 14px; color: rgba(255,255,255,.5); letter-spacing: .65px; line-height: 21.45px; }
  .appr-tt { display: flex; flex-direction: column; gap: 16px; }
  .appr h4 { font-weight: 500; font-size: 24px; color: #fff; line-height: 1.5; }
  .appr p  { font-weight: 400; font-size: 16px; color: rgba(255,255,255,.72); line-height: 1.5; max-width: 450px; }
  .appr .explore { display: flex; gap: 8px; align-items: flex-start; color: var(--gold-2); font-weight: 600; font-size: 15px; line-height: 24.75px; white-space: nowrap; flex: none; }

  /* ── Risk cards ──────────────────────────────────────────────────── */
  .risks { display: flex; gap: 24px; align-items: stretch; }
  .risk  { flex: 1 0 0; min-width: 0; background: var(--card); border: 1px solid var(--card-border); border-radius: 24px; padding: var(--container-pad); display: flex; flex-direction: column; gap: 12px; }
  .risk h4 { font-weight: 500; font-size: 20px; color: var(--ink); line-height: 1.5; }
  .risk p  { font-weight: 400; font-size: 16px; color: var(--ink-3); line-height: 1.5; }

  /* ── FAQ ─────────────────────────────────────────────────────────── */
  .faq-wrap {
    padding: 158px var(--pad);
    min-height: 100vh; min-height: 100dvh;
    display: flex; flex-direction: column; justify-content: center;
  }
  .faq-cols  { display: flex; gap: 48px; align-items: stretch; }
  .faq-left  { flex: 1 0 0; min-width: 0; display: flex; flex-direction: column; gap: 48px; }
  .faq-title { font-weight: 400; font-size: 40px; color: var(--olive-title); text-transform: capitalize; line-height: 1.5; white-space: nowrap; }
  .faq-acc   { border-radius: 24px; overflow: hidden; display: flex; flex-direction: column; gap: 2px; width: 100%; }
  .faq-item  { background: var(--card); padding: 24px; display: flex; gap: 40px; align-items: flex-start; cursor: pointer; }
  .faq-item .q { flex: 1 0 0; min-width: 0; display: flex; flex-direction: column; }
  .faq-item .qt { font-weight: 500; font-size: 14px; color: #333; line-height: 1.5; }
  .qa-wrap { display: grid; grid-template-rows: 0fr; transition: grid-template-rows .38s cubic-bezier(.4,0,.2,1); }
  .faq-item.open .qa-wrap { grid-template-rows: 1fr; }
  .qa { overflow: hidden; min-height: 0; }
  .qa p { padding-top: 16px; font-weight: 400; font-size: 14px; color: #404040; line-height: 1.5; }
  .faq-icon { width: 24px; height: 24px; flex: none; }
  .faq-icon svg { display: block; width: 24px; height: 24px; }
  .faq-icon svg .v { transform-box: fill-box; transform-origin: center; transition: transform .38s cubic-bezier(.4,0,.2,1), opacity .38s ease; }
  .faq-item.open .faq-icon svg .v { transform: scaleY(0); opacity: 0; }

  .faq-side {
    width: 464px; flex: none;
    background: var(--card); border: 1px solid var(--card-border);
    border-radius: 24px; padding: var(--container-pad);
    display: flex; flex-direction: column; gap: 40px; justify-content: flex-end;
    position: relative; overflow: hidden;
  }
  .usr {
    position: absolute; left: var(--container-pad); top: 64px;
    width: 130.667px; height: 147px; opacity: .1;
  }
  .usr img { display: block; width: 100%; height: 100%; object-fit: contain; }
  .side-in  { display: flex; flex-direction: column; gap: 32px; position: relative; }
  .side-tx  { display: flex; flex-direction: column; gap: 8px; color: #222320; }
  .side-tx h4 { font-weight: 500; font-size: 20px; line-height: 28px; letter-spacing: -0.45px; }
  .side-tx p  { font-weight: 400; font-size: 14px; line-height: 22px; letter-spacing: -0.15px; }

  /* ── Final CTA ───────────────────────────────────────────────────── */
  .cta-sec { padding: var(--section-y) var(--pad); }
  .cta-box {
    border-radius: 22px;
    padding: 64px 60px;
    display: flex; flex-direction: column; align-items: center; justify-content: center;
    gap: 32px; text-align: center;
  }
  .ct-tt { display: flex; flex-direction: column; align-items: center; gap: 16px; color: var(--olive-deep); }
  .cta-box h3 { font-weight: 500; font-size: 38px; line-height: 42.56px; letter-spacing: -0.76px; }
  .ct-sub { font-weight: 400; font-size: 17px; line-height: 28.05px; color: var(--ink-2); }

  /* ── Responsive ──────────────────────────────────────────────────── */
  @media (max-width: 1024px) {
    .cat-grid { grid-template-columns: repeat(2,1fr); }
    .cat-grid .cat:nth-child(2n)  { border-right: 0; }
    .cat-grid .cat:nth-child(7), .cat-grid .cat:nth-child(8) { border-bottom: 0; }
    .risks { flex-wrap: wrap; }
    .risk  { flex: 1 1 calc(50% - 12px); }
    .usr   { display: none; }
  }

  @media (max-width: 900px) {
    .steps { flex-direction: column; }
    .step-div { display: none; }
    .step { border-bottom: 1px solid var(--card-border); }
    .step:last-child { border-bottom: 0; }
    .faq-cols { flex-direction: column; }
    .faq-side { width: 100%; }
    .faq-title { white-space: normal; }
  }

  @media (max-width: 820px) {
    .approaches { flex-direction: column; }
    .appr p { max-width: none; }
  }

  @media (max-width: 768px) {
    :root { --pad: 16px; --container-pad: 24px; --section-y: 56px; }
    .faq-wrap { padding: 128px var(--pad); }
    .feats { padding-top: 32px; }
    .hero-inner { padding: 80px 0 48px; }
    .hero h1 { font-size: clamp(28px, 7vw, 40px); }
    .hero .lead { font-size: 16px; }
    .sec-title, .faq-title { font-size: clamp(28px, 7vw, 40px); }
    .sub-title { font-size: clamp(20px, 5.4vw, 24px); }
    .row-2 { flex-direction: column; }
    .check-list { gap: 16px; }
    .sec-lead, .sub-lead, .card-p, .cat p, .step p,
    .check-row .tx, .appr p, .risk p, .cell p { font-size: 14px; }
    .cell { padding: 12px 6px; }
    .cta-box { padding: 48px; }
    .cta-box h3 { font-size: clamp(24px, 6.4vw, 38px); line-height: 1.2; }
    .ct-sub { font-size: 15px; line-height: 1.6; }
  }

  @media (max-width: 560px) {
    .cat-grid { grid-template-columns: 1fr; }
    .cat { border-right: 0 !important; border-bottom: 1px solid var(--card-border) !important; }
    .cat:nth-child(8) { border-bottom: 0 !important; }
    .risk { flex: 1 1 100%; }
    .faq-item { gap: 20px; }
  }

  @media (prefers-reduced-motion: reduce) {
    .appr, .appr .explore span:last-child { transition: none; }
    .qa-wrap, .faq-icon svg .v { transition: none; }
  }
</style>

<script>
  /* FAQ accordion */
  document.querySelectorAll('.faq-item').forEach(function(it) {
    if (!it.querySelector('.qa-wrap')) return;
    (it as HTMLElement).addEventListener('click', function() {
      it.classList.toggle('open');
    });
  });
</script>
